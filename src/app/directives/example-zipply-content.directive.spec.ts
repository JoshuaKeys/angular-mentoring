import { ExampleZipplyContentDirective } from './example-zipply-content.directive';

describe('ExampleZipplyContentDirective', () => {
  it('should create an instance', () => {
    const directive = new ExampleZipplyContentDirective();
    expect(directive).toBeTruthy();
  });
});
